<UserControl x:Class="MovieApp.MVVM.View.TopControls.GenreSelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MovieApp.MVVM.View.TopControls" 
             mc:Ignorable="d" 
             d:DesignHeight="75" d:DesignWidth="800"
             FontFamily="../../../Fonts/#Dosis">

    <StackPanel Orientation="Horizontal">

        <TextBlock FontSize="20" Foreground="#FFFFFF" Margin="0,0,20,0"
               Text="Escolha um género:" VerticalAlignment="Center"/>

        <ComboBox ItemsSource="{Binding Genres.Genres}" Margin="0,0,20,0"
              SelectedValuePath="Id" DisplayMemberPath="Name" SelectedValue="{Binding SelectedGenre}"
              Width="300" Height="40" HorizontalAlignment="Left"
              VerticalContentAlignment="Center" FontSize="20"/>
    </StackPanel>
</UserControl>
